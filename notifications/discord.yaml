apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Provider
metadata:
  name: discord
  namespace: flux-system
spec:
  type: discord
  username: Flux CD
  channel: deployments
  secretRef:
    name: discord-deployment-webhook-url
